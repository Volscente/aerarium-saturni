import type { NextraThemeLayoutProps } from 'nextra'

// TASK-1 stub — will become the full Roman-aesthetic custom theme in TASK-2.
export default function Layout({ children }: NextraThemeLayoutProps) {
  return <>{children}</>
}
